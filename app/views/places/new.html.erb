
  <h1>New Place</h1>

  <%= form_with :model => @place do |form| %>
    <div>
      <%= form.label "name" %>
      <%= form.text_field "name" %>
    </div>
    <div>
      <%= form.submit "Submit", :class => "btn btn-primary" %>
    </div>
  <% end %>
